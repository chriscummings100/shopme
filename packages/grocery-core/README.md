# @chriscummings100/shopme-grocery-core

Core ShopMe functionality for browser attachment, vendor integrations, shopping
memory, cart/order models, and screenshots.

Most users should install the CLI package or MCP server package instead:

```bash
npx -y @chriscummings100/shopme --help
npx -y @chriscummings100/shopme-mcp-groceries
```
