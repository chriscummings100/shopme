Run the ShopMe MCP smoke test.

Follow the test plan in `c:\dev\shopme\tests\smoke-test.md` exactly, using the mcp__shopme__* tools. Execute every step in order. After each tool call, check the stated assertion. If a step fails, record the failure and the actual response, then continue with the remaining steps. At the end, produce a PASS/FAIL summary table for all 15 steps. Do not skip any steps.

Report the full PASS/FAIL summary table back to the user.
